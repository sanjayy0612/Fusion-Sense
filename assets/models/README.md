# MediaPipe model assets

Run the following command from the repository root:

```bash
python scripts/download_pose_model.py
```

This downloads Google's official Pose Landmarker Lite bundle as
`pose_landmarker_lite.task`. Model binaries are intentionally ignored by git.

